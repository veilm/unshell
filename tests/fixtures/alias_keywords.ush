rm -rf /tmp/ush-alias-test
mkdir -p /tmp/ush-alias-test
alias ccd "cd /tmp/ush-alias-test"
alias cond "if false { echo no } elif true { echo yes } else { echo never }"
alias loop "for item in one two { echo blank $item }"
alias loop_ok 'for item in one two { echo full $item }'
alias fe 'foreach item { echo $item }'
ccd
pwd
cond
loop
loop_ok
printf '%s\n' a b | fe
rm -rf /tmp/ush-alias-test

alias each foreach
seq 5 | each i { echo "i-$i" }
